version: "3.3"

services:
    api:
        build: ./src
        ports:
            - "8090:8090"
        volumes:
            - ./src:/src
        restart: always
        tty: true
        privileged: true
        command: bash
